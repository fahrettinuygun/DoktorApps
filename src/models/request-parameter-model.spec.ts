import { RequestParameterModel } from './request-parameter-model';

describe('RequestParameterModel', () => {
  it('should create an instance', () => {
    expect(new RequestParameterModel()).toBeTruthy();
  });
});
